# ─── Web client (optional) ───────────────────────────────────────
# Create React App embeds REACT_APP_* vars at build time. Leave these
# empty in development — the Axios client falls back to http://localhost:5000.

REACT_APP_API_URL=
REACT_APP_SOCKET_URL=
